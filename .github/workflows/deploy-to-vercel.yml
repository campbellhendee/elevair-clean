name: Deprecated - Legacy Vercel Deploy

on:
  workflow_dispatch:

jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This legacy workflow is deprecated and disabled. Use .github/workflows/deploy-vercel.yml instead."
